require('dotenv').config({path: './.env'});
{
   "type": "postgres",
   "host": "localhost",
   "port": 5432,
   "username": "postgres",
   "password": "postgres",
   "database": "nttdatabase",
   "synchronize": true,
   "logging": false,
   "entities": [
      "./dist/**/entity/*.entity.{js,ts}"
   ],
   "migrations": [
      "src/migration/**/*.js"
   ],
   "subscribers": [
      "src/subscriber/**/*.{js,ts}"
   ],
   "cli": {
      "entitiesDir": "src/**/entity",
      "migrationsDir": "src/_shared/migration",
      "subscribersDir": "src/subscriber"
   }
}